"""
Базовая конфигурация для микросервиса уведомлений

Архитектура:
- Общие параметры конфигурации для всех отправителей уведомлений
- Параметры повторных попыток
- Параметры валидации
- Минимальный набор параметров, не зависящих от конкретной реализации
"""
__version__ = '1.0.0'

from typing import Dict, Any

# Базовая конфигурация для всех отправителей уведомлений
BASE_NOTIFIER_CONFIG: Dict[str, Any] = {
    # Параметры отправки
    "MAX_RETRIES": 3,  # Максимальное количество попыток отправки
    "DELAY_BETWEEN_RETRIES": 1.0,  # Задержка между попытками отправки (в секундах)
    "DELAY_BETWEEN_MESSAGES": 0.1,  # Задержка между отправками сообщений (в секундах)
    "MAX_MESSAGE_LENGTH": 4096,  # Максимальная длина сообщения (в символах)
    "REQUIRED_MESSAGE_FIELDS": [],  # Обязательные поля в сообщении
    "REQUIRED_CONNECTION_PARAMS": [],  # Обязательные параметры подключения

    # Параметры пакетной обработки
    "BATCH_SIZE": 100,  # Размер пакета для пакетной отправки
    "DELAY_BETWEEN_REQUESTS": 0.1,  # Задержка между запросами при пакетной отправке

    # Параметры форматирования (по умолчанию)
    "MESSAGE_TEMPLATES": {},  # Шаблоны сообщений (передаются извне)
    "DATE_FORMAT": "%Y-%m-%d",  # Формат даты (передается извне)
    "TIME_FORMAT": "%H:%M:%S"  # Формат времени (передается извне)
}