# 🔧 Руководство по отладке парсера Ozon

## 📋 Обзор изменений

В этом документе описаны проблемы и решения, выявленные в процессе разработки и отладки парсера отчетов Ozon.

---

## 🔴 Актуальная проблема (март 2026)

### Проблема: Зависание парсера и сбой цикла выполнения (28.02.2026 — 01.03.2026)

**Симптомы:**
- **28.02**: Парсер не запускается — ошибка `SessionNotCreatedException`
- **01.03**: Парсер зависает и не завершается за отведенные 360 секунд
- Последний успешный запуск: **27.02.2026** (данные записаны в Google Sheets, уведомление отправлено)

**Причины:**

1. **Headless режим** (включён 09.02 в коммите `389cd17`)
   - Вызывал `SessionNotCreatedException: Chrome instance exited` при наличии Lock-файла
   - Lock-файл остаётся от предыдущих сессий браузера в профиле пользователя
   - В headless режиме Selenium менее стабилен при работе с существующей сессией

2. **Проверка backdrop без таймаута** (добавлена 01.03 в коммите `cafb38b`)
   - Метод `_is_backdrop_active()` использовал `find_elements()` без явного таймаута
   - Полагался на `implicit_wait` (20 секунд по умолчанию)
   - 4 селектора × 20 сек = **80 секунд на одну проверку backdrop**
   - 5+ проверок за цикл = **400+ секунд** (таймаут задачи 360 сек)

**Логи проблемы:**

```
# 28.02 — Ошибка запуска
21:30:24 ERROR Ошибка при настройке браузера Edge: Message: session not created: 
Chrome instance exited. Examine ChromeDriver verbose log to determine the cause.
21:30:24 ERROR Тип ошибки: SessionNotCreatedException
21:30:24 ERROR Параметры запуска браузера: --user-data-dir=..., headless=True
21:30:24 ERROR Файл блокировки существует: ...\Default\Lock

# 01.03 — Зависание на проверке backdrop
21:34:04 DEBUG Проверка оверлея: selector=//div[contains(@class, 'ozi__dialog__dialog__C2BB8')], timeout=5s
21:34:24 DEBUG Оверлей не обнаружен: Message: timeout
21:34:24 DEBUG Проверка backdrop: 4 селекторов из конфигурации
21:35:44 DEBUG Backdrop не активен  ← 120 секунд на проверку!
21:35:52 DEBUG Проверка оверлея: selector=...  ← следующая проверка начинается
...  ← лог обрывается, парсер не завершается
```

**Решение (коммит `04ace48` от 02.03.2026):**

1. **Отключен headless режим** (`headless: False`)
   - Файл: `configs/base_configs/base_config.py`
   - Стабильная работа с существующей сессией пользователя
   - Запуск в Windows Task Scheduler работает в фоне

2. **Добавлен таймаут 1 сек для проверки backdrop**
   - Файл: `core/ozon_report_parser.py` — метод `_is_backdrop_active()`
   - Использован `WebDriverWait` вместо `find_elements`
   - 4 селектора × 1 сек = **4 секунды на проверку**
   - Экономия: **~380 секунд за цикл парсинга**

**Результат:**
- Парсер укладывается в таймаут 360 секунд
- Данные записываются в Google Sheets
- Уведомления отправляются в Telegram

---

## 🟠 Проблема 2: Оверлей не закрывается (03.03.2026)

**Симптомы:**
- Парсер запускается, но не может закрыть оверлей
- Клик по крестику не проходит — элемент перекрыт другим оверлеем
- Ошибка: `element click intercepted: Element <button class="ozi__window__closeIcon__-pkPv"> is not clickable at point (1166, 80). Other element would receive the click: <div>...</div>`

**Причина:**
- На странице **два оверлея одновременно**:
  1. Большой оверлей "Новости Турбо ПВЗ" с кнопкой "Отложить" и крестиком
  2. Маленький оверлей уведомлений "У вас 9 непрочитанных сообщений" с кнопкой "Перейти"
- Маленький оверлей перекрывает крестик большого оверлея
- Клик по крестику перехватывается верхним оверлеем

**Структура оверлеев (HTML):**
```html
<!-- Большой оверлей -->
<div class="ozi__dialog__dialog__C2BB8">
  <div>Новости Турбо ПВЗ</div>
  <button class="ozi__button__secondary__5UTJi">Отложить</button>
  <button class="ozi__window__closeIcon__-pkPv">✕</button>  <!-- ПЕРЕКРЫТ! -->
</div>

<!-- Маленький оверлей уведомлений (перекрывает крестик) -->
<div id="ozi-notifications-container">
  <div>У вас 9 непрочитанных сообщений</div>
  <button>Перейти</button>
</div>
```

**Решение (тестирование 03.03.2026):**
- Создан тестовый скрипт `overlay_closer.py` для подбора селектора
- Протестировано 6 вариантов селекторов для кнопки "Отложить"
- **Рабочий селектор найден**: `//button[contains(@class, 'ozi__button') and normalize-space()='Отложить']`
- Изменена конфигурация в `configs/base_configs/ozon_report_config.py`

**Логи тестирования:**
```
--- Тест: postpone_text ---
[10:42:33] ❌ Ошибка: Message: timeout (не найден)

--- Тест: postpone_button ---
[10:42:40] ❌ Ошибка: Message: timeout (не найден)

--- Тест: postpone_button_class ---
[10:42:48] ❌ Ошибка: Message: timeout (не найден)

--- Тест: postpone_content ---
[10:42:55] ❌ Ошибка: Message: timeout (не найден)

--- Тест: postpone_parent ---
[10:43:03] ❌ Ошибка: Message: timeout (не найден)

--- Тест: postpone_exact ---
[10:43:05] ✓ Элемент найден и кликабелен
[10:43:05]   visible=True
[10:43:06] ✓ Клик выполнен
```

**Результат:**
- Оверлей закрывается нажатием на кнопку "Отложить"
- Парсер продолжает работу
- ПВЗ устанавливается корректно

---

## 🟡 Проблема 1: Перенаправление на страницу логина (февраль 2026)

**Основная проблема:** Периодически парсер перенаправляется на страницу логина вместо целевых страниц отчетов.

**Причина:** Сессия браузера аннулируется (пользователь вышел вручную, истек таймаут сессии).

**Решение:** Добавлено детальное логирование для быстрой диагностики + скриншоты/HTML дампы при ошибках.

**Статус:** ✅ Решено, логирование добавлено в код

---

## 🔍 Диагностика проблемы с backdrop (март 2026)

### Симптом: Парсер зависает и не завершается за 360 секунд

**Логи:**

```
21:34:04 DEBUG Проверка оверлея: selector=//div[contains(@class, 'ozi__dialog__dialog__C2BB8')], timeout=5s
21:34:24 DEBUG Оверлей не обнаружен: Message: timeout
21:34:24 DEBUG Проверка backdrop: 4 селекторов из конфигурации
21:35:44 DEBUG Backdrop не активен  ← 120 секунд на проверку!
```

**Причина:**

Метод `_is_backdrop_active()` использовал `find_elements()` без таймаута,
полагаясь на `implicit_wait` (20 секунд):

- 4 селектора × 20 сек = **80 секунд на одну проверку**
- 5+ проверок за цикл = **400+ секунд** (таймаут задачи 360 сек)

**Конфигурация backdrop_selectors:**

```python
"overlay_config": {
    "backdrop_selectors": [
        "//div[contains(@class, 'ozi__backdrop__backdrop__')]",
        "//div[contains(@class, 'ozi__backdrop')]",
        "//div[contains(@class, 'backdrop')]",
        "//div[contains(@class, 'modal-backdrop')]"
    ]
}
```

**Решение:**

Использован `WebDriverWait` с явным таймаутом 1 секунда:

```python
# Было (медленно):
backdrop_elements = self.driver.find_elements(By.XPATH, selector)

# Стало (быстро):
backdrop_timeout = 1  # секунды на каждый селектор
backdrop_elements = WebDriverWait(self.driver, backdrop_timeout).until(
    EC.presence_of_all_elements_located((By.XPATH, selector))
)
```

**Файл для исправления:**
`core/ozon_report_parser.py` — метод `_is_backdrop_active()`

**Поиск в логах:**

```bash
# Поиск длительных проверок backdrop
grep -r "Проверка backdrop:" .\logs\reports_domain\Parser\

# Поиск таймаутов backdrop (если используется WebDriverWait)
grep -r "Backdrop check timeout" .\logs\reports_domain\Parser\
```

---

## 🔍 Диагностика SessionNotCreatedException (март 2026)

### Симптом: Парсер не запускается — ошибка сессии

**Логи:**

```
21:30:24 ERROR Ошибка при настройке браузера Edge: Message: session not created: 
Chrome instance exited. Examine ChromeDriver verbose log to determine the cause.
21:30:24 ERROR Тип ошибки: SessionNotCreatedException
21:30:24 ERROR Параметры запуска браузера: --user-data-dir=C:/Users/Оператор/AppData/Local/Microsoft/Edge/User Data, headless=True
21:30:24 ERROR Файл блокировки существует: C:/Users/Оператор/AppData/Local/Microsoft/Edge/User Data\Default\Lock
```

**Причина:**

- Headless режим + Lock-файл профиля Edge = конфликт сессий
- Lock-файл остаётся от предыдущего незавершённого сеанса браузера
- В headless режиме браузер не может корректно обработать Lock-файл

**Решение:**

Отключить headless режим в конфигурации:

```python
# configs/base_configs/base_config.py
"browser_config": {
    "headless": False,  # Было: True
}
```

**Поиск в логах:**

```bash
# Поиск ошибок сессии
grep -r "SessionNotCreatedException" .\logs\reports_domain\Parser\

# Поиск Lock-файлов
grep -r "Файл блокировки" .\logs\reports_domain\Parser\
```

---

## 🎯 Новые возможности логирования

### 1. Диагностика перед навигацией

**Файл:** `core/base_report_parser.py`  
**Метод:** `navigate_to_target()`

**Что логируется:**

```
=== ДИАГНОСТИКА ПЕРЕД НАВИГАЦИЕЙ ===
Сессия драйвера: eb39d9d5d3...
Текущий URL перед переходом: https://turbo-pvz.ozon.ru/reports/giveout?...
Заголовок страницы перед переходом: Отчеты по выдачам
```

**Признак проблемы:**

```
⚠️ ВНИМАНИЕ: браузер уже находится на странице ЛОГИНА!
Возможная причина: сессия истекла или выполнен выход из системы
```

---

### 2. Диагностика после навигации

**Что логируется:**

```
=== ДИАГНОСТИКА ПОСЛЕ НАВИГАЦИИ ===
Фактический URL после перехода: https://turbo-pvz.ozon.ru/reports/giveout?...
Заголовок страницы: Отчеты по выдачам
```

**Критическая ошибка:**

```
❌ ПРОИЗОШЛО ПЕРЕНАПРАВЛЕНИЕ НА СТРАНИЦУ ЛОГИНА!
URL логина: https://turbo-pvz.ozon.ru/login?filter={...}&redirectUri=/reports/giveout?...
Перенаправление с целевой страницы: /reports/giveout?filter={...}
📸 Скриншот страницы логина сохранен: ./logs/reports_domain/Parser/error_login_20260226_213045.png
📄 HTML страницы сохранен: ./logs/reports_domain/Parser/error_login_20260226_213045.html
```

---

### 3. Диагностика страницы при проверке ПВЗ

**Файл:** `core/ozon_report_parser.py`  
**Метод:** `ensure_correct_pvz()`

**Что логируется:**

```
=== ДИАГНОСТИКА СТРАНИЦЫ ===
Текущий URL: https://turbo-pvz.ozon.ru/login?...
Заголовок страницы: Система управления заказами ПВЗ
❌ СТРАНИЦА ЛОГИНА ОБНАРУЖЕНА при проверке ПВЗ!
Сессия невалидна — возможна причина: выход пользователя или истек таймаут
Размер HTML страницы: 15234 байт
```

**Предупреждение:**

```
⚠️ ПВЗ не определен (Unknown) — возможна проблема с сессией или структурой страницы
Проверьте, что браузер находится на правильной странице, а не на странице логина
```

---

## 📁 Артефакты отладки

### Скриншоты ошибок

**Формат имени:**
- `error_login_YYYYMMDD_HHMMSS.png` — при перенаправлении на страницу логина
- `error_navigation_YYYYMMDD_HHMMSS.png` — при критических ошибках навигации
- `error_dropdown_YYYYMMDD_HHMMSS.png` — при проблеме с выбором опции в dropdown

**Расположение:** `./logs/reports_domain/Parser/`

**Когда создаются:**
- При обнаружении перенаправления на страницу логина
- При критических ошибках навигации
- При проблеме с выбором опции в dropdown (0 найдено опций)

> **Примечание:** Скриншоты создаются встроенным методом Selenium `save_screenshot()` — дополнительные пакеты не требуются!

---

### HTML дампы страниц

**Формат имени:** `error_login_YYYYMMDD_HHMMSS.html`

**Расположение:** `./logs/reports_domain/Parser/`

**Когда создаются:**
- При перенаправлении на страницу логина

**Зачем нужно:**
- Полный DOM страницы для анализа структуры
- Возможность открыть в браузере и изучить элементы
- Поиск изменений в CSS-классах, селекторах

---

## 🔍 Анализ типичных проблем

### Проблема 1: Перенаправление на login

**Симптомы в логе:**

```
21:30:20 DEBUG Проверка оверлея: selector=//div[contains(@class, 'ozi__dialog__dialog__C2BB8')], timeout=5s
21:30:40 DEBUG Оверлей не обнаружен
21:31:01 DEBUG Поиск элемента по селектору: //input[contains(@class, 'ozi__input__input__ie7wU') and @readonly]
21:31:21 DEBUG Ошибка при получении значения элемента ... no such element
21:31:21 DEBUG Сессия драйвера активна: eb39d9d5d3...
21:31:21 DEBUG Текущий URL: https://turbo-pvz.ozon.ru/login?...
21:31:21 DEBUG Заголовок страницы: Система управления заказами ПВЗ
```

**Причина:** Сессия аннулирована (пользователь вышел, истек таймаут)

**Решение:** Ручная авторизация в системе

---

### Проблема 2: Элемент ПВЗ не найден

**Симптомы в логе:**

```
21:30:40 DEBUG Требуемый ПВЗ: ЧЕБОКСАРЫ_340
21:30:40 DEBUG Используемый селектор для получения ПВЗ: //input[contains(@class, 'ozi__input__input__ie7wU') and @readonly]
21:31:01 DEBUG Ошибка при получении значения элемента ... no such element
21:31:01 DEBUG Текущий ПВЗ: Unknown
```

**Возможные причины:**
1. Страница логина вместо целевой (см. Проблему 1)
2. Изменились CSS-классы элементов
3. Структура страницы изменилась

**Диагностика:**
- Проверить `Текущий URL` — если содержит `login`, см. Проблему 1
- Открыть сохраненный HTML файл, проверить наличие элемента
- Сравнить селекторы с актуальной версией страницы

---

### Проблема 3: Оверлей не закрывается

**Симптомы в логе:**

```
21:30:20 DEBUG Проверка оверлея: selector=//div[contains(@class, 'ozi__dialog__dialog__C2BB8')], timeout=5s
21:30:40 DEBUG Оверлей не обнаружен: Message: timeout
```

**Возможные причины:**
1. Изменился CSS-класс оверлея
2. Это другой тип модального окна
3. Окно не является оверлеем (например, push-уведомление)

**Диагностика:**
- Открыть скриншот ошибки
- Изучить HTML на наличие модальных элементов
- Предложить новый селектор

---

## 🛠️ Инструменты для анализа

### Просмотр скриншотов

```bash
# Windows
start .\logs\reports_domain\Parser\error_login_20260226_213045.png
```

### Анализ HTML

```bash
# Открыть в браузере
start .\logs\reports_domain\Parser\error_login_20260226_213045.html

# Или в редакторе для поиска селекторов
code .\logs\reports_domain\Parser\error_login_20260226_213045.html
```

### Поиск в логах

```bash
# Поиск перенаправлений на login
grep -r "СТРАНИЦА ЛОГИНА" .\logs\reports_domain\Parser\

# Поиск ошибок элементов
grep -r "no such element" .\logs\reports_domain\Parser\

# Поиск предупреждений
grep -r "⚠️" .\logs\reports_domain\Parser\
```

---

## 📊 Примеры логов

### ✅ Успешная навигация

```
21:30:19 DEBUG Переходим на URL: https://turbo-pvz.ozon.ru/reports/giveout?filter={...}
21:30:19 DEBUG === ДИАГНОСТИКА ПЕРЕД НАВИГАЦИЕЙ ===
21:30:19 DEBUG Сессия драйвера: eb39d9d5d3...
21:30:19 DEBUG Текущий URL перед переходом: https://turbo-pvz.ozon.ru/
21:30:19 DEBUG Заголовок страницы перед переходом: Главная
21:30:20 DEBUG === ДИАГНОСТИКА ПОСЛЕ НАВИГАЦИИ ===
21:30:20 DEBUG Фактический URL после перехода: https://turbo-pvz.ozon.ru/reports/giveout?filter={...}
21:30:20 DEBUG Заголовок страницы: Отчеты по выдачам
21:30:20 DEBUG Навигация выполнена успешно, URL сохранен в конфиг
```

### ❌ Ошибка: страница логина

```
21:30:19 DEBUG Переходим на URL: https://turbo-pvz.ozon.ru/reports/giveout?filter={...}
21:30:19 DEBUG === ДИАГНОСТИКА ПЕРЕД НАВИГАЦИЕЙ ===
21:30:19 DEBUG Сессия драйвера: eb39d9d5d3...
21:30:19 DEBUG Текущий URL перед переходом: https://turbo-pvz.ozon.ru/login?...
21:30:19 DEBUG ⚠️ ВНИМАНИЕ: браузер уже находится на странице ЛОГИНА!
21:30:20 DEBUG === ДИАГНОСТИКА ПОСЛЕ НАВИГАЦИИ ===
21:30:20 DEBUG Фактический URL после перехода: https://turbo-pvz.ozon.ru/login?...
21:30:20 DEBUG ❌ ПРОИЗОШЛО ПЕРЕНАПРАВЛЕНИЕ НА СТРАНИЦУ ЛОГИНА!
21:30:20 DEBUG URL логина: https://turbo-pvz.ozon.ru/login?filter={...}&redirectUri=/reports/giveout?...
21:30:20 DEBUG Перенаправление с целевой страницы: /reports/giveout?filter={...}
21:30:20 ERROR 📸 Скриншот страницы логина сохранен: ./logs/reports_domain/Parser/error_login_20260226_213020.png
21:30:20 ERROR 📄 HTML страницы сохранен: ./logs/reports_domain/Parser/error_login_20260226_213020.html
```

---

### ✅ Успешная работа с dropdown (новая диагностика v1.1)

```
12:57:21 DEBUG Поиск опций по селектору: //div[contains(@class, 'ozi__dropdown-item__dropdownItem__cDZcD')...]
12:57:21 DEBUG === ДИАГНОСТИКА DROPDOWN ===
12:57:21 DEBUG Dropdown элемент: aria-expanded='None', class='ozi__input__root__ie7wU ozi__input-select_...'
12:57:21 DEBUG Найдено опций: 4
12:57:21 DEBUG === СПИСОК ВСЕХ ДОСТУПНЫХ ОПЦИЙ ===
12:57:21 DEBUG   Опция #1: текст='СОСНОВКА_10...', id='', aria-selected='None'
12:57:21 DEBUG     class='ozi__dropdown-item__dropdownItem__cDZcD...'
12:57:21 DEBUG   Опция #2: текст='ЧЕБОКСАРЫ_182...', id='', aria-selected='None'
12:57:21 DEBUG   Опция #3: текст='ЧЕБОКСАРЫ_144...', id='', aria-selected='None'  ← ЦЕЛЕВАЯ
12:57:21 DEBUG   Опция #4: текст='ЧЕБОКСАРЫ_143...', id='', aria-selected='None'
12:57:21 DEBUG Найдена частичная опция: ЧЕБОКСАРЫ_144
12:57:21 DEBUG Выбрана опция: ЧЕБОКСАРЫ_144
```

---

### ✅ Успешная диагностика оверлея (новая диагностика v1.1)

```
12:56:37 DEBUG Проверка оверлея: selector=//div[contains(@class, 'ozi__dialog__dialog__C2BB8')], timeout=5s
12:56:37 DEBUG === ДИАГНОСТИКА ОВЕРЛЕЯ ===
12:56:37 DEBUG Найдено элементов по селектору: 1
12:56:37 DEBUG   Элемент #1: id='', class='ozi__dialog__dialog__C2BB8...', displayed=False
12:56:37 DEBUG     style='display: none;...'
12:56:37 DEBUG Оверлей найден, но не виден (скрыт)
12:56:37 DEBUG Оверлей не обнаружен на странице
```

---

### ❌ Ошибка: не найдена опция в dropdown (со скриншотом)

```
12:41:41 DEBUG === ДИАГНОСТИКА DROPDOWN ===
12:41:41 DEBUG Dropdown элемент: aria-expanded='None', class='ozi__input__root__ie7wU...'
12:41:41 DEBUG Найдено опций: 0
12:41:41 DEBUG === СПИСОК ВСЕХ ДОСТУПНЫХ ОПЦИЙ ===
12:41:41 DEBUG Попытка найти опции альтернативными селекторами...
12:41:41 DEBUG   Альтернативный селектор '//div[contains(@class, 'ozi__dropdown-item')]' нашел 0 опций
12:42:01 DEBUG ❌ НЕ НАЙДЕНА опция для значения 'ЧЕБОКСАРЫ_144'
12:42:01 DEBUG Всего найдено опций: 0
12:42:01 ERROR 📸 Скриншот dropdown сохранен: ./logs/reports_domain/Parser/error_dropdown_20260227_124201.png
```

---

## 🎓 Рекомендации по улучшению отладки

### 1. Включите подробное логирование

```bash
python scheduler_runner/tasks/reports/reports_processor.py --detailed_logs
```

### 2. Сохраняйте артефакты

Не удаляйте скриншоты и HTML файлы сразу — они могут понадобиться для анализа повторяющихся проблем.

### 3. Сравнивайте с успешными логами

При анализе проблемы сравнивайте логи неудачного запуска с успешными логами других объектов.

### 4. Документируйте находки

Если обнаружена новая причина сбоя, добавьте её в этот документ.

---

## 📞 Эскалация проблем

При обнаружении повторяющихся проблем:

1. Соберите логи за проблемный период
2. Приложите скриншоты и HTML дампы
3. Опишите контекст (время, действия пользователей)
4. Обратитесь к разработчику

---

**Версия документа:** 1.3
**Дата:** 2026-02-27
**Дата обновления:** 2026-03-03 (v1.3)

---

## 📝 История изменений

### v1.3 (2026-03-03)
- Добавлена проблема с оверлеем (кнопка "Отложить" вместо крестика)
- Описано тестирование 6 селекторов кнопки "Отложить"
- Добавлена структура HTML оверлеев
- Документирован скрипт overlay_closer.py для тестирования селекторов
- Указан рабочий селектор: `//button[contains(@class, 'ozi__button') and normalize-space()='Отложить']`

### v1.2 (2026-03-02)
- Добавлена диагностика проблемы с зависанием на проверке backdrop
- Добавлено описание SessionNotCreatedException в headless режиме
- Обновлены рекомендации по конфигурации (headless: False)
- Добавлен таймаут 1 сек для проверки backdrop (WebDriverWait)
- Описаны коммиты: `389cd17` (headless), `cafb38b` (backdrop), `04ace48` (fix)
- Добавлены примеры логов с реальными таймингами (120 сек на backdrop)

### v1.1 (2026-02-27)
- Добавлена детальная диагностика dropdown (список всех опций, альтернативные селекторы)
- Добавлена детальная диагностика оверлея (атрибуты, стиль, количество элементов)
- Добавлено сохранение скриншота при проблеме с dropdown
- Обновлены примеры успешного выполнения

### v1.0 (2026-02-27)
- Начальная версия документа
