"""
Базовая конфигурация для BaseParser

Содержит стандартные параметры для работы с веб-браузером Edge через Selenium.
Является базовой конфигурацией, от которой наследуются конфигурации дочерних классов.
Дочерние конфигурации могут переопределять значения по умолчанию, указанные в этом файле.

=== ОПИСАНИЕ ПАРАМЕТРОВ ===

Параметры браузера:
- EDGE_USER_DATA_DIR: Путь к пользовательским данным Edge (если пусто, используется шаблон)
- HEADLESS: Режим headless для браузера
- BROWSER_EXECUTABLE: Имя исполняемого файла браузера
- BROWSER_USER_DATA_PATH_TEMPLATE: Шаблон пути к данным браузера

Параметры конфигурации браузера:
- browser_config.user_data_dir: Путь к пользовательским данным (если пусто, используется EDGE_USER_DATA_DIR)
- browser_config.headless: Режим headless (переопределяет глобальный HEADLESS)
- browser_config.window_size: Размер окна браузера [ширина, высота]
- browser_config.timeout: Таймаут ожидания элементов (переопределяет DEFAULT_TIMEOUT)

Таймауты:
- DEFAULT_TIMEOUT: Таймаут ожидания элементов (секунды)
- ELEMENT_CLICK_TIMEOUT: Таймаут ожидания кликабельности элемента (секунды)
- ELEMENT_WAIT_TIMEOUT: Таймаут ожидания появления элемента (секунды)

Паузы:
- PROCESS_TERMINATION_SLEEP: Время ожидания после завершения процессов браузера (секунды)
- BROWSER_CLOSE_DELAY: Время задержки перед закрытием браузера (секунды, 0 - без задержки)

Примечание:
- Параметры, связанные с конкретной функциональностью (например, table_configs)
  должны быть определены в более специфичных конфигурациях (например, в ozon_report_config.py)
- Этот файл служит только для общих параметров, применимых ко всем типам парсеров
"""
__version__ = '0.0.1'

# Базовая конфигурация для BaseParser
BASE_CONFIG = {
    # === ПАРАМЕТРЫ БРАУЗЕРА ===
    "EDGE_USER_DATA_DIR": "",  # Путь к пользовательским данным Edge (если пусто, используется шаблон)
    "HEADLESS": False,  # Режим headless для браузера
    "BROWSER_EXECUTABLE": "msedge.exe",  # Имя исполняемого файла браузера
    "BROWSER_USER_DATA_PATH_TEMPLATE": "C:/Users/{username}/AppData/Local/Microsoft/Edge/User Data",  # Шаблон пути к данным браузера

    # === ПАРАМЕТРЫ КОНФИГУРАЦИИ БРАУЗЕРА ===
    "browser_config": {
        "user_data_dir": "",  # Путь к пользовательским данным (если пусто, используется EDGE_USER_DATA_DIR)
        "headless": True,  # False, браузер Edge запускается в видимом режиме, True - в фоне
        "window_size": [1920, 1080],  # Размер окна браузера [ширина, высота]
        "timeout": 20,  # Таймаут ожидания элементов (переопределяет DEFAULT_TIMEOUT)
    },

    # === ТАЙМАУТЫ ===
    "DEFAULT_TIMEOUT": 20,  # Таймаут ожидания элементов (секунды)
    "ELEMENT_CLICK_TIMEOUT": 10,  # Таймаут ожидания кликабельности элемента (секунды)
    "ELEMENT_WAIT_TIMEOUT": 10,  # Таймаут ожидания появления элемента (секунды)

    # === ПАУЗЫ ===
    "PROCESS_TERMINATION_SLEEP": 2,  # Время ожидания после завершения процессов браузера (секунды)
    "BROWSER_CLOSE_DELAY": 0,  # Время задержки перед закрытием браузера (секунды, 0 - без задержки)
    "PAGE_LOAD_DELAY": 3,  # Задержка после загрузки страницы (секунды)
    "PAGE_UPDATE_DELAY": 2,  # Задержка после обновления страницы (секунды)
    "DROPDOWN_OPEN_DELAY": 2,  # Задержка после открытия выпадающего списка (секунды)
    "ELEMENT_SEARCH_DELAY": 1,  # Задержка перед поиском элемента (секунды)

    # === ДОПОЛНИТЕЛЬНЫЕ ПАРАМЕТРЫ ===
    "logger": None,  # Объект логгера (если требуется)

}