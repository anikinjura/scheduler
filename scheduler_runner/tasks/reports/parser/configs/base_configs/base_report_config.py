"""
Конфигурация для BaseReportParser

Содержит параметры для работы с отчетами, валидацией, форматированием и сохранением данных.
Наследует все параметры из BASE_CONFIG, включая параметры использования сессии.
Является базовой конфигурацией, от которой наследуются конфигурации дочерних классов.
Дочерние конфигурации могут переопределять значения по умолчанию, указанные в этом файле.

=== ОПИСАНИЕ ПАРАМЕТРОВ ===

Параметры работы с отчетами:
- xml_root_element: Имя корневого элемента XML
- xml_item_prefix: Префикс для элементов списка в XML
- default_output_dir: Путь по умолчанию для сохранения отчетов
- filename_template: Шаблон имени файла
- schema_keys: Ключи для получения информации из схемы
- supported_field_types: Поддерживаемые типы данных

Параметры даты:
- date_format: Формат даты по умолчанию
- datetime_format: Формат даты и времени по умолчанию

Параметры вывода:
- output_config: Конфигурация вывода данных

Параметры валидации:
- validation_config: Конфигурация валидации данных

Параметры мульти-шаговой обработки:
- multi_step_config: Структура для поддержки многошаговой обработки данных

Примечание:
- Параметры, связанные с конкретной функциональностью (например, селекторы, фильтры)
  должны быть определены в более специфичных конфигурациях (например, в ozon_report_config.py)
- Этот файл служит только для общих параметров, применимых ко всем типам отчетных парсеров
"""
__version__ = '0.0.1'

# Импортируем базовую конфигурацию
from .base_config import BASE_CONFIG

# Конфигурация для BaseReportParser
BASE_REPORT_CONFIG = {
    **BASE_CONFIG,  # Наследуем все параметры из базовой конфигурации

    # === ПАРАМЕТРЫ РАБОТЫ С ОТЧЕТАМИ ===
    "xml_root_element": "report",  # Имя корневого элемента XML
    "xml_item_prefix": "item_",  # Префикс для элементов списка в XML
    "default_output_dir": "./output",  # Путь по умолчанию для сохранения отчетов
    "filename_template": "{report_type}_report_{timestamp}.{output_format}",  # Шаблон имени файла
    "schema_keys": {  # Ключи для получения информации из схемы
        "required_fields": "required_fields",
        "field_types": "field_types"
    },
    "supported_field_types": {  # Поддерживаемые типы данных
        "string": str,
        "integer": int,
        "float": float,
        "boolean": bool,
        "list": list,
        "dict": dict
    },

    # === ДОПОЛНИТЕЛЬНЫЕ ПАРАМЕТРЫ ОТЧЕТОВ ===
    "output_config": {
        "dir": "./output",  # Директория для сохранения отчетов
        "format": "json",  # Формат вывода по умолчанию
        "encoding": "utf-8"  # Кодировка файлов
    },

    # === ПАРАМЕТРЫ ВАЛИДАЦИИ ===
    "validation_config": {
        "strict_mode": False,  # Строгий режим валидации (останавливать при ошибках)
        "log_errors": True,  # Логировать ошибки валидации
        "skip_invalid_fields": True  # Пропускать невалидные поля вместо остановки
    },

}